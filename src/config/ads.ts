export const ADS_ENABLED = true;
// Set to false if you want to disable AdMob during debugging.

// Use Google test ad unit IDs when not in production
export const ADS_TEST_MODE = (import.meta as any)?.env?.MODE !== 'production';
